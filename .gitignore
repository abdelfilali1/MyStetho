# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so

# Virtual environments
.venv/
venv/
env/
ENV/

# Local tools / temp
.claude/
.codex_tmp/

# Secrets & environnement (NE JAMAIS COMMITER — token WhatsApp, etc.)
.env
.env.local
medfollow/.env
run_whatsapp_local.ps1
*.local.ps1
Token Whatsapp
Token*Whatsapp*

# Rapport d'audit sécurité : cartographie fichier:ligne de failles non corrigées
# de l'app en production. Le dépôt est public — ce fichier reste local.
2. Improvments/Doctivo_Security_Audit.md

# Node
node_modules/

# OS / editor
.DS_Store
Thumbs.db

# Application runtime data (patient data — never commit)
medfollow/data/*.db
medfollow/data/*.db.backup-*
medfollow/data/.secret_key
medfollow/data/feuilles/
medfollow/uploads/
medfollow/server.out.log
medfollow/server.err.log

# Céphalométrie IA : gros binaires servis en statique (modèle ONNX + runtime WASM).
# Trop volumineux pour le dépôt ; livrés/déployés séparément. Le code JS reste suivi.
medfollow/static/js/ceph/model/
medfollow/static/js/ceph/ort/*.wasm

# Radio IA : modèle ONNX servi au navigateur (généré par scripts/export_onnx.py),
# et poids PyTorch d'origine. Hors dépôt (comme la céphalo) ; livrés par le tarball
# de déploiement. Le runtime ORT est réutilisé depuis static/js/ceph/ort/.
medfollow/static/js/radio/model/
medfollow/models/dentalxray/
