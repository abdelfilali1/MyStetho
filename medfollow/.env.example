# Copy this file to .env and fill in the values.
# On Railway: set these as environment variables in your project settings.

# REQUIRED for production — prevents session invalidation on redeploy
MEDFOLLOW_SECRET_KEY=change-me-to-a-random-64-char-hex-string

# Optional overrides
# HOST=0.0.0.0
# PORT=8000
# MEDFOLLOW_DATABASE_PATH=/data/medfollow.db
